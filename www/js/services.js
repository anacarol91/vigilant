angular.module('starter.services', [])
/*
.factory('Infracoes', function() {

	var infracoes = [{
		id: 01,
		lat:,
		lng:,
		img:,
		placa:,
		descricao:,
		data:
	}];

	return {
    all: function() {
      return infracoes;
    },
    remove: function(infracao) {
      infracoes.splice(infracoes.indexOf(infracao), 1);
    },
    get: function(infracaoId) {
      for (var i = 0; i < infracoes.length; i++) {
        if (infracoes[i].id === parseInt(infracaoId)) {
          return infracoes[i];
        }
      }
      return null;
    }
  };

});



.factory('Infracao', function() {

	var infracoes = [{
		id:,
		lat:,
		lng:,
	}];

});*/